<Window x:Class="Hud.App.Views.LeakSpotListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="APH - Spots de leak"
        Height="760"
        Width="1420"
        MinHeight="620"
        MinWidth="1180"
        Background="{StaticResource Brush.Bg}"
        WindowStartupLocation="CenterOwner">

  <Window.Resources>
    <Style TargetType="TextBlock">
      <Setter Property="Foreground" Value="{StaticResource Brush.Text}"/>
      <Setter Property="TextWrapping" Value="Wrap"/>
    </Style>
    <Style TargetType="DataGrid">
      <Setter Property="Background" Value="#101720"/>
      <Setter Property="Foreground" Value="{StaticResource Brush.Text}"/>
      <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}"/>
      <Setter Property="GridLinesVisibility" Value="All"/>
      <Setter Property="HorizontalGridLinesBrush" Value="#223244"/>
      <Setter Property="VerticalGridLinesBrush" Value="#223244"/>
      <Setter Property="RowBackground" Value="#111821"/>
      <Setter Property="AlternatingRowBackground" Value="#171F2B"/>
      <Setter Property="HeadersVisibility" Value="Column"/>
      <Setter Property="CanUserAddRows" Value="False"/>
      <Setter Property="CanUserDeleteRows" Value="False"/>
      <Setter Property="CanUserResizeRows" Value="False"/>
      <Setter Property="IsReadOnly" Value="True"/>
      <Setter Property="AutoGenerateColumns" Value="False"/>
      <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>
    <Style TargetType="DataGridColumnHeader">
      <Setter Property="Background" Value="#E9EEF3"/>
      <Setter Property="Foreground" Value="Black"/>
      <Setter Property="FontWeight" Value="Bold"/>
      <Setter Property="HorizontalContentAlignment" Value="Center"/>
      <Setter Property="Padding" Value="6,5"/>
    </Style>
    <Style TargetType="DataGridCell">
      <Setter Property="Foreground" Value="{StaticResource Brush.Text}"/>
      <Setter Property="Padding" Value="6,3"/>
    </Style>

    <DataTemplate x:Key="CardChipListTemplate">
      <ItemsControl ItemsSource="{Binding}">
        <ItemsControl.ItemsPanel>
          <ItemsPanelTemplate>
            <StackPanel Orientation="Horizontal"/>
          </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <Border Width="20"
                    Height="24"
                    Margin="1,0"
                    CornerRadius="3"
                    Background="{Binding Background}"
                    BorderBrush="#506176"
                    BorderThickness="1">
              <TextBlock Text="{Binding Rank}"
                         Foreground="{Binding Foreground}"
                         FontSize="15"
                         FontWeight="Black"
                         TextAlignment="Center"
                         HorizontalAlignment="Center"
                         VerticalAlignment="Center"/>
            </Border>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </DataTemplate>

    <DataTemplate x:Key="NetBbCellTemplate">
      <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
        <TextBlock Text="{Binding NetBbTrendIcon}" Foreground="{Binding NetBbTrendBrush}" FontWeight="Bold" Margin="0,0,4,0"/>
        <TextBlock Text="{Binding NetBbLabel}" TextWrapping="NoWrap"/>
      </StackPanel>
    </DataTemplate>
  </Window.Resources>

  <Grid Margin="14">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <Border Background="{StaticResource Brush.Card}"
            BorderBrush="{StaticResource Brush.Border}"
            BorderThickness="1"
            CornerRadius="8"
            Padding="16"
            Margin="0,0,0,12">
      <DockPanel>
        <StackPanel DockPanel.Dock="Left">
          <TextBlock Text="{Binding Title}" FontSize="24" FontWeight="Bold"/>
          <TextBlock Text="{Binding Summary}" Foreground="{StaticResource Brush.TextDim}" Margin="0,4,0,0"/>
        </StackPanel>
        <StackPanel DockPanel.Dock="Right" HorizontalAlignment="Right">
          <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,8">
            <TextBlock Text="{Binding CountLabel}" FontSize="18" FontWeight="Bold" Margin="0,0,18,0"/>
            <TextBlock Text="{Binding TotalBbLabel}" FontSize="18" FontWeight="Bold" Foreground="{Binding TotalBbBrush}"/>
          </StackPanel>
          <UniformGrid Columns="4" MinWidth="520">
            <StackPanel Margin="4,0">
              <TextBlock Text="Pos" Foreground="{StaticResource Brush.TextDim}" FontSize="11"/>
              <ComboBox x:Name="PositionFilter" Height="26" Foreground="Black" SelectionChanged="Filter_Changed"/>
            </StackPanel>
            <StackPanel Margin="4,0">
              <TextBlock Text="Bote" Foreground="{StaticResource Brush.TextDim}" FontSize="11"/>
              <ComboBox x:Name="PotFilter" Height="26" Foreground="Black" SelectionChanged="Filter_Changed"/>
            </StackPanel>
            <StackPanel Margin="4,0">
              <TextBlock Text="Real" Foreground="{StaticResource Brush.TextDim}" FontSize="11"/>
              <ComboBox x:Name="ResultFilter" Height="26" Foreground="Black" SelectionChanged="Filter_Changed"/>
            </StackPanel>
            <StackPanel Margin="4,0">
              <TextBlock Text="{DynamicResource Common.Sort}" Foreground="{StaticResource Brush.TextDim}" FontSize="11"/>
              <ComboBox x:Name="SortFilter" Height="26" Foreground="Black" SelectionChanged="Filter_Changed"/>
            </StackPanel>
          </UniformGrid>
        </StackPanel>
      </DockPanel>
    </Border>

    <DataGrid Grid.Row="1" ItemsSource="{Binding Hands}" MouseDoubleClick="HandsGrid_MouseDoubleClick">
      <DataGrid.Columns>
        <DataGridTextColumn Header="{DynamicResource Grid.Date}" Binding="{Binding DateStack}" Width="82"/>
        <DataGridTextColumn Header="Eff.Stack" Binding="{Binding EffectiveStackLabel}" Width="78"/>
        <DataGridTextColumn Header="Spot" Binding="{Binding Spot}" Width="115"/>
        <DataGridTextColumn Header="Pos" Binding="{Binding Position}" Width="58"/>
        <DataGridTemplateColumn Header="{DynamicResource Common.Hero}" Width="74">
          <DataGridTemplateColumn.CellTemplate>
            <DataTemplate>
              <ContentControl Content="{Binding HeroCards}" ContentTemplate="{StaticResource CardChipListTemplate}" HorizontalAlignment="Center"/>
            </DataTemplate>
          </DataGridTemplateColumn.CellTemplate>
        </DataGridTemplateColumn>
        <DataGridTextColumn Header="P.Line" Binding="{Binding PreflopLine}" Width="72"/>
        <DataGridTemplateColumn Header="Flop" Width="96">
          <DataGridTemplateColumn.CellTemplate>
            <DataTemplate>
              <ContentControl Content="{Binding FlopCardChips}" ContentTemplate="{StaticResource CardChipListTemplate}" HorizontalAlignment="Center"/>
            </DataTemplate>
          </DataGridTemplateColumn.CellTemplate>
        </DataGridTemplateColumn>
        <DataGridTextColumn Header="F.Line" Binding="{Binding FlopLine}" Width="70"/>
        <DataGridTemplateColumn Header="Turn" Width="58">
          <DataGridTemplateColumn.CellTemplate>
            <DataTemplate>
              <ContentControl Content="{Binding TurnCardChips}" ContentTemplate="{StaticResource CardChipListTemplate}" HorizontalAlignment="Center"/>
            </DataTemplate>
          </DataGridTemplateColumn.CellTemplate>
        </DataGridTemplateColumn>
        <DataGridTextColumn Header="T.Line" Binding="{Binding TurnLine}" Width="70"/>
        <DataGridTemplateColumn Header="River" Width="58">
          <DataGridTemplateColumn.CellTemplate>
            <DataTemplate>
              <ContentControl Content="{Binding RiverCardChips}" ContentTemplate="{StaticResource CardChipListTemplate}" HorizontalAlignment="Center"/>
            </DataTemplate>
          </DataGridTemplateColumn.CellTemplate>
        </DataGridTemplateColumn>
        <DataGridTextColumn Header="R.Line" Binding="{Binding RiverLine}" Width="70"/>
        <DataGridTextColumn Header="Combo" Binding="{Binding MadeHand}" Width="100"/>
        <DataGridTextColumn Header="Draw" Binding="{Binding DrawLabel}" Width="90"/>
        <DataGridTextColumn Header="{DynamicResource Common.Result}" Binding="{Binding RealLabel}" Width="76"/>
        <DataGridTemplateColumn Header="Î”bb" CellTemplate="{StaticResource NetBbCellTemplate}" Width="72"/>
        <DataGridTextColumn Header="Motivo" Binding="{Binding Reason}" Width="*"/>
      </DataGrid.Columns>
    </DataGrid>
  </Grid>
</Window>



