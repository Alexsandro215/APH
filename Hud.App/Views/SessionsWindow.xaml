<Window x:Class="Hud.App.Views.SessionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="APH - Sesiones"
        Height="620"
        Width="980"
        MinHeight="480"
        MinWidth="760"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource Brush.AppShell}">

  <Window.Resources>
    <Style TargetType="TextBlock">
      <Setter Property="Foreground" Value="{DynamicResource Brush.Text}"/>
      <Setter Property="TextWrapping" Value="Wrap"/>
    </Style>
    <Style TargetType="DataGrid">
      <Setter Property="Background" Value="{DynamicResource Brush.Card}"/>
      <Setter Property="Foreground" Value="{DynamicResource Brush.Text}"/>
      <Setter Property="RowBackground" Value="{DynamicResource Brush.Card}"/>
      <Setter Property="AlternatingRowBackground" Value="{DynamicResource Brush.Panel}"/>
      <Setter Property="BorderBrush" Value="{DynamicResource Brush.Border}"/>
      <Setter Property="GridLinesVisibility" Value="Horizontal"/>
      <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource Brush.Border}"/>
      <Setter Property="HeadersVisibility" Value="Column"/>
      <Setter Property="RowHeight" Value="34"/>
      <Setter Property="IsReadOnly" Value="True"/>
      <Setter Property="CanUserAddRows" Value="False"/>
      <Setter Property="CanUserDeleteRows" Value="False"/>
      <Setter Property="AutoGenerateColumns" Value="False"/>
    </Style>
    <Style TargetType="DataGridColumnHeader">
      <Setter Property="Background" Value="{DynamicResource Brush.Header}"/>
      <Setter Property="Foreground" Value="{StaticResource FgLight}"/>
      <Setter Property="FontWeight" Value="SemiBold"/>
      <Setter Property="Padding" Value="8,5"/>
      <Setter Property="BorderBrush" Value="{DynamicResource Brush.HeaderBorder}"/>
      <Setter Property="BorderThickness" Value="0,0,1,1"/>
    </Style>
    <Style TargetType="DataGridCell">
      <Setter Property="Foreground" Value="{DynamicResource Brush.Text}"/>
      <Setter Property="BorderBrush" Value="{DynamicResource Brush.Border}"/>
      <Setter Property="Padding" Value="8,4"/>
    </Style>
  </Window.Resources>

  <Grid Margin="20">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <Border Grid.Row="0"
            Background="{DynamicResource Brush.Panel}"
            BorderBrush="{DynamicResource Brush.Border}"
            BorderThickness="1"
            CornerRadius="8"
            Padding="18"
            Margin="0,0,0,14">
      <DockPanel>
        <StackPanel>
          <TextBlock Text="Sesiones"
                     FontSize="28"
                     FontWeight="Black"/>
          <TextBlock x:Name="FolderText"
                     Foreground="{DynamicResource Brush.TextDim}"
                     Margin="0,4,0,0"/>
        </StackPanel>
        <StackPanel DockPanel.Dock="Right"
                    Orientation="Horizontal"
                    VerticalAlignment="Center">
          <Button Content="Abrir carpeta"
                  Style="{StaticResource Btn.Primary}"
                  Width="120"
                  Margin="0,0,8,0"
                  Click="BtnOpenFolder_Click"/>
          <Button Content="Actualizar"
                  Style="{StaticResource Btn.Primary}"
                  Width="110"
                  Click="BtnRefresh_Click"/>
        </StackPanel>
      </DockPanel>
    </Border>

    <TextBlock Grid.Row="1"
               x:Name="StatusText"
               Foreground="{DynamicResource Brush.Accent}"
               Margin="4,0,0,10"/>

    <DataGrid Grid.Row="2"
              x:Name="SessionsGrid"
              MouseDoubleClick="SessionsGrid_MouseDoubleClick">
      <DataGrid.Columns>
        <DataGridTextColumn Header="No. sesion" Binding="{Binding SessionNumber}" Width="90"/>
        <DataGridTextColumn Header="Inicio" Binding="{Binding StartLabel}" Width="160"/>
        <DataGridTextColumn Header="Cierre" Binding="{Binding CloseLabel}" Width="160"/>
        <DataGridTextColumn Header="Mesas" Binding="{Binding TableCount}" Width="70"/>
        <DataGridTextColumn Header="Duracion" Binding="{Binding DurationLabel}" Width="100"/>
        <DataGridTextColumn Header="Heroe" Binding="{Binding Hero}" Width="130"/>
        <DataGridTextColumn Header="Archivo" Binding="{Binding FileName}" Width="*"/>
      </DataGrid.Columns>
    </DataGrid>
  </Grid>
</Window>
